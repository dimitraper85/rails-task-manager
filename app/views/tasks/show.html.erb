<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>Is this task completed? <%= @task.completed %></p>
<div><%= link_to "See all tasks", tasks_path%></div>
<div><%= link_to "Delete this task", task_path(@task),
        data:{turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
</div>
